<script setup lang="ts"></script>

<template>
  <section>
    <h2>Devices settings</h2>
    <p>
      This page lives under a <code>(admin)</code> route group — the folder
      name is ignored for URL matching, so this resolves to
      <code>/devices/settings</code>, not <code>/devices/admin/settings</code>.
    </p>
  </section>
</template>
